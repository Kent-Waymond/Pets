import { tuple } from '../_util/type';

export const CommonComponentStates = tuple(
  'default',
  'primary',
  'success',
  'info',
  'danger',
  'warning',
);
export const CommonComponentSizes = tuple('large', 'default', 'small');
