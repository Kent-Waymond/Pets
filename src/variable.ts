// fishpond 所有model
export enum EnumProjectModels {
  global = 'global',
  account = 'account',
  dashboard = 'dashboard',
  instance = 'instance',
  node = 'node',
}

// 默认table分页大小
export const TABLE_PAGE_SIZE = 50;
